# Senior Angular Interview Workbook

This file contains **50 Beginner + 100 Intermediate + 150 Senior/Expert** questions. Every row includes the fields required by the brief. Answers are intentionally compact; follow links to the topic files for full reasoning.

## Beginner bank — 50

| ID | Question | Expected answer | Why interviewer asks | Common wrong answer | Follow-up question | Difficulty |
|---|---|---|---|---|---|---|
| B01 | What is an Angular component? | A directive with a template, metadata, host, view, state and behavior. | Tests core mental model. | “A TypeScript class” only. | How is its view different from its host? | Beginner |
| B02 | What does `@Component` do? | Supplies compiler metadata such as selector, template, imports, providers and host configuration. | Separates compile metadata from runtime instance. | “Creates the DOM at runtime.” | Which metadata creates a DI scope? | Beginner |
| B03 | Are standalone components recommended? | Yes; they are default/recommended for new code and import template dependencies directly. | Detects obsolete NgModule-only knowledge. | “Every component needs an NgModule.” | Are NgModules deprecated? | Beginner |
| B04 | What starts a standalone app? | `bootstrapApplication(root, config)` creates application providers and root view. | Checks modern bootstrap. | `platformBrowserDynamic().bootstrapModule` only. | What does `ApplicationConfig` contain? | Beginner |
| B05 | What is interpolation? | `{{expr}}` binds a template expression into a text/string context with escaping. | Binding basics/security. | “Raw HTML injection.” | How does `[innerHTML]` differ? | Beginner |
| B06 | Property vs attribute binding? | Property binds live DOM/directive state; `attr.` writes/removes an HTML/ARIA attribute. | Common DOM confusion. | “They are identical.” | Why can `[attr.disabled]="false"` mislead? | Beginner |
| B07 | What is event binding? | `(event)="handler($event)"` listens to a DOM event or Angular output. | Verifies template direction. | Square brackets. | Do Angular outputs bubble? | Beginner |
| B08 | What does two-way syntax mean? | `[(x)]` combines `[x]` and `(xChange)` or a framework model directive. | Tests API contract. | “Angular watches any variable automatically.” | How does `model()` support it? | Beginner |
| B09 | What is `@if`? | Compiler-native conditional view syntax recommended over deprecated `NgIf`. | Current syntax. | “A JavaScript if inside HTML.” | When is its `as` alias useful? | Beginner |
| B10 | Why does `@for` require `track`? | To map logical items to reusable views/DOM and minimize correct updates. | Performance/identity. | “Only to remove a warning.” | When is `$index` safe? | Beginner |
| B11 | What does `@empty` do? | Renders its block when the `@for` iterable has no items. | Current template fluency. | Handles HTTP errors. | Which `$` locals exist in `@for`? | Beginner |
| B12 | What is a template reference variable? | A view-scoped reference to an element/directive/component, declared with `#`. | View model basics. | Persistent component state. | How do `exportAs` values affect it? | Beginner |
| B13 | What is a signal? | A synchronously readable reactive current-value primitive invoked as a getter. | Modern state baseline. | “An Observable replacement.” | How does Angular track reads? | Beginner |
| B14 | `set` vs `update`? | `set` replaces with a value; `update` computes from the current value. | Mutation fluency. | `update` is asynchronous. | What equality is used by default? | Beginner |
| B15 | What is `computed()`? | A lazy, memoized, read-only derivation with dynamically tracked dependencies. | Core signal reasoning. | “An effect that writes state.” | When does it recompute? | Beginner |
| B16 | What is `effect()` for? | Side effects syncing signals to imperative non-reactive APIs; it is a last-choice API, not derivation. | Prevents signal misuse. | Any calculated value. | Name a valid use case. | Beginner |
| B17 | Why expose readonly signals? | Preserve one mutation owner and invariants while allowing reactive reads. | Encapsulation. | It deep-freezes objects. | How can nested mutation still occur? | Beginner |
| B18 | What is dependency injection? | Consumers request runtime tokens; hierarchical injectors resolve providers and cache scoped values. | Framework foundation. | Global service locator only. | What is a provider? | Beginner |
| B19 | What does `inject()` do? | Resolves a token from the current injection context. | Modern DI syntax. | Works from any function at any time. | Where is injection context available? | Beginner |
| B20 | What does `@Service()` mean in v22? | Modern root-provided, tree-shakable service shorthand using `inject()` dependencies; opt out for manual scope. | Latest DI awareness. | Same as a component provider. | When keep `@Injectable`? | Beginner |
| B21 | What is `providedIn: 'root'`? | Registers a tree-shakable provider in the root environment/module injector lifetime. | Service scope. | Instantiates immediately. | How can a component override it? | Beginner |
| B22 | Why use `InjectionToken`? | Interfaces/primitives have no unique runtime class token; it provides typed runtime identity. | TS/DI boundary. | The description string is the identity. | Can two same-description tokens match? | Beginner |
| B23 | Component vs directive? | A component is a directive that owns a template; other directives attach behavior to a host. | Taxonomy. | Directives always manipulate DOM manually. | What is structural behavior? | Beginner |
| B24 | What is a pure pipe? | A deterministic template transform rerun when primitive values or input references change. | Template performance. | Runs on every check. | Why can `array.push` stay stale? | Beginner |
| B25 | What does `AsyncPipe` do? | Subscribes, exposes latest value, marks view for check, switches sources and unsubscribes on destroy. | Subscription safety. | Converts Observable to Promise. | When is manual subscribe valid? | Beginner |
| B26 | Template-driven vs Reactive Forms? | Template directives/mutable model suit simple forms; explicit typed control trees scale to complex forms. | Form choice. | Reactive Forms use signals automatically. | Where is each source of truth? | Beginner |
| B27 | What is `FormControl`? | A control value plus validation, interaction and disabled/pending state. | Form primitive. | A DOM input element. | What is `FormGroup`? | Beginner |
| B28 | `setValue` vs `patchValue`? | Complete structure versus partial update. | Prevents hidden contract errors. | They are aliases. | Which can hide misspelled fields? | Beginner |
| B29 | What does `dirty` mean? | UI changed the control since pristine state; not necessarily unequal to initial value. | Form semantics. | Invalid. | How does `touched` differ? | Beginner |
| B30 | What are Signal Forms? | Stable v22 signal-model forms using `form()`, field trees, `[formField]` and schema rules. | Latest forms. | Deprecated Reactive Forms. | When might Reactive Forms remain better? | Beginner |
| B31 | What is `RouterOutlet`? | Placeholder where the router activates the matched route component view. | Router basics. | A link. | Can layouts have nested outlets? | Beginner |
| B32 | Path vs query parameter? | Path identifies route/resource hierarchy; query commonly represents optional filter/sort/paging. | URL design. | Query params are not shareable. | Which should hold a selected user ID? | Beginner |
| B33 | What does `loadComponent` do? | Dynamically imports a standalone component for lazy route loading. | Modern lazy routing. | Loads it from an API. | What can defeat the lazy chunk? | Beginner |
| B34 | What is a route guard? | A navigation decision returning a guard result; it is not server-side security. | Security awareness. | An authorization boundary. | Why return `UrlTree` instead of navigate? | Beginner |
| B35 | Guard vs resolver? | Guard decides navigation; resolver obtains data before activation. | Router lifecycle. | Both render loading UI. | What is resolver's UX trade-off? | Beginner |
| B36 | What is `HttpClient`? | Angular's typed Observable HTTP client with interceptors, XSRF integration and testing support. | HTTP foundation. | Runtime JSON validator. | Is it injectable by default in v22? | Beginner |
| B37 | Does `http.get<User>()` validate JSON? | No; the generic is a compile-time TypeScript assertion. | Runtime trust. | Yes, it rejects wrong shapes. | Where should validation occur? | Beginner |
| B38 | Why can one HTTP call execute twice? | HttpClient Observables are normally cold; two subscriptions start two requests. | RxJS/HTTP baseline. | Angular always retries once. | How would you share with an explicit cache policy? | Beginner |
| B39 | What is an HTTP interceptor? | Middleware around request/response Observable flow for cross-cutting transport concerns. | HTTP architecture. | A route guard. | Which style is recommended? | Beginner |
| B40 | Observable vs Promise? | Observable can produce 0..N notifications and has teardown/operators; Promise settles once and is eager once created. | Async basics. | Observable is always asynchronous. | Which does HttpClient return? | Beginner |
| B41 | Subject vs Observable? | Subject is both observer and multicast Observable; ordinary Observable need not expose pushing. | RxJS ownership. | Every Observable is a Subject. | Why hide `.next()`? | Beginner |
| B42 | BehaviorSubject's distinction? | Requires a seed and synchronously gives current/latest value to a subscriber. | Legacy state fluency. | Replays an unlimited history. | When prefer a signal? | Beginner |
| B43 | What does `map` do? | Transforms each emitted value, not the Observable's timing/concurrency. | Operator basics. | Starts nested requests automatically. | What operator maps to HTTP and flattens? | Beginner |
| B44 | What does `switchMap` do? | Switches to the latest inner Observable and unsubscribes the previous one. | Search/cancellation. | Runs all requests in parallel. | When is cancellation wrong? | Beginner |
| B45 | Why unsubscribe? | Long-lived subscriptions can retain observers/resources and keep side effects after owner destruction. | Lifecycle safety. | Every HttpClient request leaks. | Name automatic cleanup approaches. | Beginner |
| B46 | What is change detection? | Synchronizing eligible view bindings/state with renderer/DOM. | Runtime foundation. | A deep object diff. | Does a signal write immediately render? | Beginner |
| B47 | What is the v22 default strategy? | `OnPush`; Eager/Default is opt-in. | Detects stale interview material. | Default/Eager. | What notifies an OnPush view? | Beginner |
| B48 | Is Zone.js default in v22? | No; Angular is zoneless by default since v21. | Current runtime. | Required for every async update. | What replaces its broad notifications? | Beginner |
| B49 | What is SSR? | Angular renders HTML per request on a server, then the client may hydrate it. | Rendering baseline. | Static build only. | How does SSG differ? | Beginner |
| B50 | What is hydration? | Reusing server-rendered DOM and attaching client view/listeners instead of recreating it. | Modern rendering. | Downloading lazy JavaScript only. | What causes hydration mismatch? | Beginner |

## Intermediate bank — 100

| ID | Question | Expected answer | Why interviewer asks | Common wrong answer | Follow-up question | Difficulty |
|---|---|---|---|---|---|---|
| I001 | Why are standalone imports explicit? | The compiler needs each template dependency; locality simplifies composition, lazy loading and removal without declaration modules. | Modern architecture. | They import JavaScript globally. | Can standalone import an NgModule? | Intermediate |
| I002 | What does `providers` on a component do? | Configures its host ElementInjector, creating a scoped instance per component occurrence for descendants. | DI lifetime. | Registers one app singleton. | What happens on destroy? | Intermediate |
| I003 | `providers` vs `viewProviders`? | `providers` can be visible to view and projected content; `viewProviders` hides values from projected content. | Projection/DI nuance. | Identical aliases. | Give an isolation use case. | Intermediate |
| I004 | Emulated vs ShadowDom encapsulation? | Emulated rewrites selectors/attributes; ShadowDom uses native shadow roots; neither replaces deliberate global CSS policy. | Styling reality. | Emulated creates Shadow DOM. | What does `None` do? | Intermediate |
| I005 | `input.required` runtime guarantee? | It gives compile-time binding enforcement/type and throws if read before supplied; it cannot validate arbitrary runtime data. | Type vs runtime. | It validates JSON. | When can early read fail? | Intermediate |
| I006 | When use input transforms? | Pure representation coercion such as boolean/number attributes, not business validation or side effects. | API design. | Fetch data inside transform. | Why must transform be pure? | Intermediate |
| I007 | How does `model()` create two-way binding? | It is writable input state plus implicit `nameChange` output emitted on set/update. | Component contracts. | It shares the parent's object by DI. | When use explicit input/output instead? | Intermediate |
| I008 | Why avoid output names like `click`? | They collide conceptually with native events and outputs do not have identical DOM bubbling semantics. | API clarity. | Angular forbids them. | Do outputs bubble? | Intermediate |
| I009 | How does content projection ownership work? | Parent owns projected view; child supplies slots. Content query can see it; view query searches child's own template. | View model. | Child owns all projected nodes. | Why not conditionally render `ng-content`? | Intermediate |
| I010 | What is `<ng-template>`? | An inert template fragment represented by `TemplateRef`, instantiated as embedded views by control flow/directives/outlets. | Structural mechanics. | Hidden DOM element. | What owns created views? | Intermediate |
| I011 | Why is `@for track item.id` better than identity? | IDs persist across refetched object instances, preserving views/state and minimizing moves/creates. | Rendering identity. | It makes iteration O(1). | What if IDs duplicate? | Intermediate |
| I012 | When is `track $index` incorrect? | Insertions, deletes or reorder change index-to-entity mapping, reusing stateful DOM for the wrong item. | Practical UI correctness. | It is always fastest. | Where is it acceptable? | Intermediate |
| I013 | How does `@if (...; as x)` help types/performance? | Evaluates/aliases an expression in that branch and narrows truthy value, avoiding repeated long reads. | Template fluency. | Creates mutable variable. | Is the alias hoisted? | Intermediate |
| I014 | What can template expressions access? | Component members, lexical template locals and limited supported globals/operators; not arbitrary JS global scope. | Compiler model. | Any browser global. | How handle `window` with SSR? | Intermediate |
| I015 | Why avoid methods in templates? | They can be reevaluated on each relevant check; side effects are incorrect and expensive work should be cached/derived. | Performance. | Angular calls each method once. | When is a tiny pure method acceptable? | Intermediate |
| I016 | How does signal equality work? | Writes compare old/new with `Object.is` unless a custom equality function says equivalent; equivalent writes do not notify. | Fine-grained behavior. | Deep comparison always. | Risk of custom deep equality? | Intermediate |
| I017 | What happens when computed dependency branch changes? | The next execution records only signals actually read; old edges are removed and new edges added. | Dynamic tracking. | Dependencies are fixed at creation. | Explain the hidden-count example. | Intermediate |
| I018 | Why is `computed` lazy? | Dependency writes invalidate cached value; computation runs when a consumer reads after invalidation. | Prediction skill. | Runs immediately on every write. | What gets logged on two reads? | Intermediate |
| I019 | Component effect vs root effect? | Component effect runs in component synchronization/lifecycle; root effect runs as microtask outside component tree timing. | v22 signal nuance. | They are always synchronous. | Why does placement matter? | Intermediate |
| I020 | How is effect cleanup registered? | The callback receives `onCleanup`, called before rerun/destroy; its injection context normally owns final destruction. | Resource safety. | Return a subscription directly. | When use manualCleanup? | Intermediate |
| I021 | What does `untracked` do? | Reads a signal/callback without adding its reads to the active reactive consumer. | Graph control. | Prevents a signal update. | How can misuse create staleness? | Intermediate |
| I022 | Why are reads after `await` not tracked? | Reactive context is stack/synchronous; it ends at the async boundary. | Internals. | Angular patches promises to retain it. | How ensure dependency tracking? | Intermediate |
| I023 | `linkedSignal` vs `computed`? | Both derive; linked is writable and resets/reconciles when source changes, computed is always read-only. | State choice. | Linked is an Observable. | Give selection-list use case. | Intermediate |
| I024 | Resource status model? | idle/loading/reloading/resolved/local/error plus value/error/loading/snapshot signals. | Async state. | Only loading boolean. | Why distinguish reload from load? | Intermediate |
| I025 | Why not perform mutations in `resource`? | Param changes/destroy cancel loaders; aborting a write can leave uncertain server state and auto-reexecution can duplicate it. | Correctness. | POST is unsupported syntactically. | How model commands? | Intermediate |
| I026 | `resource` vs `httpResource`? | General Promise/abort loader versus HttpClient-backed request with interceptors/testing/response metadata. | Modern API selection. | httpResource bypasses interceptors. | When use `rxResource`? | Intermediate |
| I027 | `toSignal` subscription timing? | It subscribes immediately; reuse one conversion, provide initial value or require synchronous emission. | Interop lifecycle. | Subscribes only when signal is read. | What happens on error? | Intermediate |
| I028 | Why can `toObservable` skip intermediate signal writes? | It propagates through scheduled effect after stabilization; multiple earlier writes can coalesce to latest. | Push/pull nuance. | Observable buffers all signal history. | How preserve every event? | Intermediate |
| I029 | EnvironmentInjector vs ElementInjector? | Environment scopes app/routes/dynamic environments; element scopes component/directive host hierarchy. Resolution searches both in phases. | DI internals. | They are same tree. | Which route API creates one? | Intermediate |
| I030 | Closest-provider rule? | First matching token in resolution order wins; it determines instance/config visible to the request. | Hierarchical reasoning. | Root always wins. | How deliberately access parent? | Intermediate |
| I031 | `useExisting` vs `useClass`? | Existing aliases one cached instance; class provider creates/resolves a provider record that can yield another instance. | Provider semantics. | Both aliases. | How expose two tokens to one logger? | Intermediate |
| I032 | What is a multi provider? | Multiple registrations contribute to an injected array under one token; all participants must consistently use `multi`. | Extensibility. | Last provider wins. | Name an Angular example. | Intermediate |
| I033 | Why cannot interfaces be DI tokens? | TypeScript erases them; DI needs runtime identity. | TS/runtime boundary. | Interfaces are private. | Abstract class or token trade-off? | Intermediate |
| I034 | What does `skipSelf` mean? | Start lookup at the parent injector, often to decorate/extend a parent-provided value. | Resolution modifiers. | Skip root. | How does `self` differ? | Intermediate |
| I035 | What does `host` limit? | Stops element lookup at the host boundary for the requesting directive/component context. | Advanced DI. | Search only document body. | Combine with optional? | Intermediate |
| I036 | Why can `inject()` fail in `ngOnInit`? | Lifecycle method execution is outside construction injection context. Capture dependency in a field or pass/run context deliberately. | Common runtime error. | OnInit is too early. | Where is `inject` valid? | Intermediate |
| I037 | Tree-shakable provider meaning? | Compiler/bundler can omit automatic provider/service code when no reachable consumer uses it. It does not mean lazy instance equals lazy chunk. | Bundle reasoning. | Every root service is a lazy chunk. | How can direct providers affect reachability? | Intermediate |
| I038 | Why component-provide workflow state? | Lifetime and isolation match each rendered workflow instance; destruction cleans it. | Scope design. | For faster DI. | When choose route scope instead? | Intermediate |
| I039 | Pure vs impure pipe execution? | Pure reacts to argument value/reference changes; impure can run on every eligible view check. | Performance. | Pure means no DI. | Better alternative to impure filter? | Intermediate |
| I040 | How does AsyncPipe switch sources? | On bound reference change it unsubscribes old, subscribes new, exposes latest and marks view for check. | Template/RxJS. | Subscribes to both forever. | Why stabilize Observable references? | Intermediate |
| I041 | Typed Forms nullable default? | `FormControl('')` is `string|null` because reset may produce null; nonNullable changes type/reset behavior. | Forms precision. | Strict mode removes null. | How use NonNullableFormBuilder? | Intermediate |
| I042 | Why disabled controls disappear from group `.value`? | Disabled controls are excluded from ancestor aggregate value/validation; `getRawValue` includes them. | Real form bugs. | Their value is erased. | Security implication? | Intermediate |
| I043 | FormArray vs FormRecord? | Ordered dynamic homogeneous controls versus open-ended keyed homogeneous controls. | Dynamic forms. | Both are JavaScript arrays. | What handles fixed heterogeneous keys? | Intermediate |
| I044 | Async validator execution order? | Sync validators first; async only if sync passes, control becomes pending until finite result completes. | API load/correctness. | Both always run concurrently. | Why must Observable complete? | Intermediate |
| I045 | Why set `updateOn: 'blur'`? | Delay value/validation updates, often reducing expensive async validation per keystroke. | Performance/UX. | Makes input uncontrolled. | When is change better? | Intermediate |
| I046 | Signal Forms field tree? | Navigable mirror of model structure; calling a node returns reactive field state and value signal. | Latest forms. | A FormGroup wrapper copy. | Who is source of truth? | Intermediate |
| I047 | Signal Forms pending nuance? | While pending, `valid` and `invalid` can both be false; inspect `pending` explicitly. | Exact status semantics. | Pending equals invalid. | How disable submit? | Intermediate |
| I048 | Why plain models for Signal Forms? | Structural walking/copying expects plain objects/arrays; classes/Map/Set can lose prototypes or form empty/incorrect trees. | Boundary design. | TypeScript rejects classes automatically. | How integrate domain classes? | Intermediate |
| I049 | Why route configuration order matters? | Router uses first matching route; a broad parameter/wildcard can shadow later specific routes. | Debugging. | Chooses most specific automatically. | Where place wildcard? | Intermediate |
| I050 | Why `pathMatch: 'full'` on empty redirect? | Empty prefix matches every URL; full prevents redirecting unrelated paths. | Classic routing bug. | It enables nested routes. | What is default match? | Intermediate |
| I051 | `CanMatch` vs `CanActivate`? | `CanMatch` participates while choosing a route and false lets matching continue; `CanActivate` protects activation after a route is selected. | Router precision. | Both always cancel navigation. | Why replace `CanLoad` with `CanMatch`? | Intermediate |
| I052 | Why must guards not be authorization boundaries? | Client code and state are controllable; the server must enforce permissions for every protected operation. | Security maturity. | A hidden route is secure. | What may a guard safely improve? | Intermediate |
| I053 | Snapshot vs observable route params? | Snapshot is one point in time; streams/signals update when a reused component receives new route state. | Reuse bugs. | Components are always recreated. | When is snapshot sufficient? | Intermediate |
| I054 | What does `withComponentInputBinding()` do? | Binds route data, resolved values, and parameters to matching component inputs. | Modern router API. | Creates two-way URL binding. | How handle missing optional values? | Intermediate |
| I055 | Resolver trade-off? | It can guarantee data before activation but adds navigation latency; use it for route-critical data, not every request. | UX judgment. | Resolvers always improve perceived speed. | How surface a resolver error? | Intermediate |
| I056 | What does lazy `loadComponent` buy? | The component and reachable code can become a separate chunk fetched on navigation. | Bundle design. | It creates a new injector automatically. | How does `loadChildren` differ? | Intermediate |
| I057 | Route provider lifetime? | Providers on a route live in that route's EnvironmentInjector and are shared by its activated subtree until destroyed. | Scope ownership. | One instance per component render. | Give a feature-store use case. | Intermediate |
| I058 | `replaceUrl` vs `skipLocationChange`? | Replace overwrites the current history entry; skip avoids adding the target URL to browser history/address behavior. | Navigation UX. | Both reload the page. | Which suits post-login redirect? | Intermediate |
| I059 | How cancel stale search HTTP calls? | Drive terms through `switchMap`; unsubscription aborts the in-flight HttpClient request where supported. | Concurrency correctness. | Nest subscriptions. | When would `exhaustMap` be better? | Intermediate |
| I060 | Why configure `provideHttpClient()`? | Add functional interceptors, XSRF/JSONP/parent-request behavior or other features; basic HttpClient is injectable by default in current Angular. | Version awareness. | Import deprecated HttpClientModule in every feature. | What backend is default in v22? | Intermediate |
| I061 | Functional vs DI-based interceptor? | Functional interceptors have explicit order and use `inject`; Angular recommends them for more predictable behavior. | Modern HTTP design. | Functional interceptors cannot inject. | Where is their injection context? | Intermediate |
| I062 | Why is interceptor order important? | Requests flow in registration order and responses unwind in reverse, so auth, retry, cache and logging interactions differ. | Middleware reasoning. | All run in parallel. | Put retry before or after metrics? | Intermediate |
| I063 | Is HttpClient response JSON validated? | No; the generic is a compile-time assertion. Validate untrusted payloads at the boundary. | Runtime safety. | `get<User>()` parses and validates User. | Where should mapping live? | Intermediate |
| I064 | `observe: 'response'` use? | Access status, headers and body rather than body alone. Literal option types may need `as const`. | HTTP fluency. | Enables retries. | How observe progress events? | Intermediate |
| I065 | Why avoid mutating `HttpRequest` in an interceptor? | Requests/headers/params are immutable; clone with changes so retries and chained interceptors remain safe. | API correctness. | TypeScript readonly is optional. | Is the body deeply immutable? | Intermediate |
| I066 | How does HttpClient cancellation work? | Unsubscribing tears down the request; `switchMap`, AsyncPipe, or resource destruction can trigger it. | Lifecycle correctness. | Promises cancel automatically. | What about resource AbortSignal? | Intermediate |
| I067 | `shareReplay` cache hazard? | Long-lived replay can retain subscription/value, cache errors or stale data unless reset/refCount/invalidation semantics are deliberate. | RxJS production issue. | It is a complete application cache. | Design invalidation. | Intermediate |
| I068 | Cold vs hot Observable? | Cold creates producer work per subscription; hot multicasts an independently/shared producer. Classification depends on construction. | RxJS mental model. | All HTTP is hot. | What does `share` change? | Intermediate |
| I069 | `switchMap` vs `concatMap`? | Switch cancels prior inner work; concat preserves order and queues. Choose by domain semantics. | Concurrency selection. | Switch is simply faster. | Which for autosave writes? | Intermediate |
| I070 | `mergeMap` concurrency risk? | Concurrent inner work can complete out of order and overload resources; cap concurrency or choose another flattening rule. | Race awareness. | Results retain input order. | Which operator ignores re-entry? | Intermediate |
| I071 | `exhaustMap` use case? | Ignore new triggers while one inner operation runs, useful for duplicate-submit prevention. | UX concurrency. | Cancel current submit for newest. | What user feedback is needed? | Intermediate |
| I072 | `catchError` placement effect? | Inside a flattening operator recovers one inner failure and keeps outer input alive; outside can terminate the whole chain. | Stream resilience. | Placement never matters. | When rethrow? | Intermediate |
| I073 | Why avoid nested subscriptions? | They scatter ownership/error/cancellation and create races; composition expresses one lifecycle. | RxJS discipline. | Nested code is only stylistic. | Refactor with `switchMap`. | Intermediate |
| I074 | `combineLatest` initial emission rule? | It waits until each source has emitted, then emits on any change using latest values. | Blank UI debugging. | Emits immediately with undefined. | How seed a source? | Intermediate |
| I075 | `forkJoin` completion rule? | It emits once after every input completes; a never-completing input means no result. | Aggregation choice. | Same as combineLatest. | Why suitable for finite HTTP? | Intermediate |
| I076 | Why use `takeUntilDestroyed()`? | It binds subscription teardown to a DestroyRef without maintaining a manual subject. | Lifecycle safety. | It retries until destroy. | Where must injection context exist? | Intermediate |
| I077 | AsyncPipe or `toSignal`? | AsyncPipe is direct template consumption; toSignal provides reusable synchronous component logic. Avoid repeated conversions/subscriptions. | Interop judgment. | Always convert everything to signals. | How supply initial state? | Intermediate |
| I078 | What marks an OnPush view dirty? | Input binding change, handled event, explicit mark, template-consumed signal update, AsyncPipe notification and related framework paths. | CD debugging. | Only immutable input changes. | Does object mutation notify it? | Intermediate |
| I079 | Why can mutable input state render stale? | Reference equality may not change and no notification may schedule/check the child; replace the reference or expose reactive state. | Common UI defect. | OnPush deep-compares. | Can a local event mask it? | Intermediate |
| I080 | `markForCheck` vs `detectChanges`? | Mark schedules/flags normal checking; detectChanges synchronously checks the local view subtree and should be used sparingly. | CD API judgment. | They are aliases. | Which does AsyncPipe use? | Intermediate |
| I081 | How does v22 OnPush-default affect new code? | Components start targeted rather than eager checking; opt into `ChangeDetectionStrategy.Default` only when needed. | Current default. | OnPush is still optional optimization only. | What migration bugs can appear? | Intermediate |
| I082 | What schedules zoneless change detection? | Known Angular notifications such as signal updates read in templates, markForCheck, setInput, handled listeners and attaching dirty views. | Modern runtime. | A global timer poll. | How diagnose missed third-party updates? | Intermediate |
| I083 | Why remove Zone.js only after an audit? | Code may rely on patched async tasks to trigger rendering; convert boundaries to explicit Angular notifications first. | Migration safety. | Zoneless changes only bundle size. | What does the zoneless provider do in current Angular? | Intermediate |
| I084 | `afterNextRender` vs `ngAfterViewInit`? | Render callbacks run after Angular finishes DOM rendering and are browser-only; AfterViewInit is a lifecycle hook and immediate writes can cause expression errors. | DOM timing. | Both run during SSR. | When use phased callbacks? | Intermediate |
| I085 | Why avoid layout reads and writes interleaved? | They can force repeated synchronous layout; phase writes before reads to reduce thrashing. | Browser performance. | Angular batches all browser layout automatically. | Name a measurable symptom. | Intermediate |
| I086 | `@defer` dependency eligibility? | A dependency must be standalone and referenced only inside the defer block in that file to be truly deferred. | Bundle debugging. | Any import inside a block lazy-loads. | What remains eager? | Intermediate |
| I087 | Placeholder vs loading block in `@defer`? | Placeholder appears before trigger/fetch; loading appears while deferred dependencies load, with timing controls to prevent flicker. | UX correctness. | They are synonymous. | What does minimum do? | Intermediate |
| I088 | Why add a prefetch trigger? | Fetch code/data before the display trigger while keeping rendering deferred, reducing interaction latency. | Performance design. | Prefetch renders the block. | Give viewport/idle combination. | Intermediate |
| I089 | What does hydration reconcile? | Client Angular reuses server-rendered DOM and associates it with application views instead of discarding/recreating it. | SSR fundamentals. | It only downloads JavaScript. | What causes mismatch? | Intermediate |
| I090 | Incremental hydration relation to `@defer`? | Server can render defer content and client hydrates it at configured triggers; event replay protects early interaction. | Current SSR. | Deferred blocks never render on server. | How opt out globally? | Intermediate |
| I091 | Why is direct DOM access risky for SSR? | Browser globals may not exist and DOM mutation can create hydration mismatch; isolate browser-only work after render/platform checks. | Universal design. | Domino always emulates everything. | What belongs in a browser adapter? | Intermediate |
| I092 | Unit vs component integration test? | Unit isolates logic; component integration renders template with Angular DI/change detection and catches contract errors. | Test strategy. | TestBed tests are end-to-end. | Which gives best confidence for a CVA? | Intermediate |
| I093 | Why call `fixture.detectChanges()`? | Run binding, initial lifecycle and rendering; subsequent calls flush eligible synchronous view updates. | Testing mechanics. | It only queries DOM. | How do signals affect it? | Intermediate |
| I094 | `fakeAsync` limitation with Vitest? | Native Vitest fake timers are preferred; zone-based fakeAsync requires the compatibility patch and can conflict with timer mocking. | Current runner knowledge. | Karma APIs work identically everywhere. | How test async more portably? | Intermediate |
| I095 | Why prefer behavior assertions over private calls? | They preserve refactor freedom and validate the observable contract across template, DI and lifecycle. | Test quality. | Private methods need 100% direct coverage. | When is a pure helper worth direct testing? | Intermediate |
| I096 | What does `HttpTestingController` verify? | It captures pending requests so tests can assert URL/method/body, flush responses/errors and verify none remain. | HTTP test correctness. | It starts a real mock server. | Why call `verify()`? | Intermediate |
| I097 | How does Angular sanitize templates? | It treats bound values as untrusted and sanitizes supported HTML/URL contexts; resource URLs cannot generally be sanitized. | Security baseline. | All strings are safe after escaping once. | What is a security context? | Intermediate |
| I098 | Why is `bypassSecurityTrustHtml` dangerous? | It disables Angular's protection for that value; only vetted trusted content should cross that narrow boundary. | XSS judgment. | It sanitizes more strongly. | Where centralize the review? | Intermediate |
| I099 | AOT benefits beyond speed? | Templates compile ahead of time, catching errors and avoiding a runtime compiler; it also supports optimization and reduces injection surface. | Build/security. | It encrypts source code. | Is JIT appropriate in production? | Intermediate |
| I100 | What does the application builder use? | The modern CLI build system uses esbuild and an encapsulated Vite-based dev server; apps should not depend on Vite internals. | Toolchain currency. | Angular CLI still requires webpack configuration. | Why is direct Vite config unsupported? | Intermediate |
